apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: akka-kubernetes
  name: akka-kubernetes
  namespace: akka-kubernetes
spec:
  replicas: 3
  selector:
    matchLabels:
     app: akka-kubernetes
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate

  template:
    metadata:
      labels:
        app: akka-kubernetes
    spec:
      containers:
      - name: akka-kubernetes
        image: chbatey/akka-kubernetes:latest
        livenessProbe:
          tcpSocket:
            port: 8558
        ports:
        - name: remoting
          containerPort: 2552
          protocol: TCP
        - name: management
          containerPort: 8558
          protocol: TCP
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace

---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: pod-reader
  namespace: akka-kubernetes
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "watch", "list"]
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: read-pods
  namespace: akka-kubernetes
subjects:
- kind: User
  name: system:serviceaccount:akka-kubernetes:default
roleRef:
  kind: Role
  name: pod-reader
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: akka-kubernetes
  name: akka-kubernetes
  namespace: akka-kubernetes
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: akka-kubernetes
  type: LoadBalancer
---
# Can likely be changed to an Ingress
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: akka-kubernetes
  name: akka-kubernetes
  namespace: akka-kubernetes
spec:
  host: akka-kubernetes.centralpark.lightbend.com
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: akka-kubernetes
    weight: 100
  wildcardPolicy: None
status:
  ingress:
  - conditions:
    host: akka-kubernetes.centralpark.lightbend.com
    routerName: router
    wildcardPolicy: None
